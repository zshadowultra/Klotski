export const CONFIG = {
  // The corner radius of the pieces in pixels.
  // This value is used across all screen sizes to maintain a consistent look.
  // You can experiment with this value (e.g., 8, 12, 16, 20).
  pieceRadius: 22,
};
