export const CONFIG = {
  // The corner radius of the pieces.
  // This value acts as a baseline (at a standard 70px cell size) and scales
  // proportionally with the screen size to maintain a consistent shape
  // (e.g., a rounded square won't turn into a circle on small screens).
  // You can experiment with this value (e.g., 8, 12, 16, 22, 35).
  pieceRadius: 22,
};
